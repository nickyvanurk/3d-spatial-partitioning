import * as THREE from "three";

export class Fleet {
    size: number;
    radius: number;

    constructor(size: number, radius: number) {
        this.size = size;
        this.radius = radius;
    }

    reset() {
        // empty
    }

    update(dt: number) {
        // empty
    }

    render(alpha: number) {
        // empty
    }
}